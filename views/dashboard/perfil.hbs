<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - {{viewTitle}}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Estilos Generales y Layout */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f6f9; /* Color de fondo del contenido */
            display: flex;
            min-height: 100vh;
        }

        /* BARRA LATERAL (SIDEBAR) */
        .sidebar {
            width: 250px;
            background-color: #2c3e50; /* Azul oscuro */
            color: white;
            padding-top: 20px;
            box-shadow: 2px 0 5px rgba(0,0,0,0.1);
            display: flex;
            flex-direction: column;
            flex-shrink: 0; /* Asegura que no se encoja */
        }

        .logo {
            text-align: center;
            padding: 10px 0 30px;
            font-size: 1.5em;
            font-weight: bold;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            margin-bottom: 20px;
        }

        .sidebar nav {
            flex-grow: 1; 
        }

        .sidebar a {
            display: block;
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            padding: 15px 20px;
            transition: background-color 0.3s, color 0.3s;
        }

        .sidebar a:hover, .sidebar a.active {
            background-color: #34495e; 
            color: white;
            border-left: 3px solid #3498db; 
        }

        .sidebar a i {
            margin-right: 10px;
            width: 20px;
            text-align: center;
        }

        /* Botón de Cerrar Sesión */
        .logout-section {
            padding: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logout-btn {
            display: block;
            width: 100%;
            padding: 10px 0;
            background-color: #e74c3c; 
            color: white;
            text-align: center;
            text-decoration: none;
            border-radius: 4px;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .logout-btn:hover {
            background-color: #c0392b;
        }

        /* CONTENIDO PRINCIPAL */
        .main-content {
            flex-grow: 1;
            padding: 20px 30px;
            box-sizing: border-box;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0 20px;
            border-bottom: 1px solid #ddd;
            margin-bottom: 20px;
        }

        .header h1 {
            margin: 0;
            color: #333;
        }
        
        /* Contenedor principal blanco para el contenido */
        .dashboard-body {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            min-height: 80vh; 
        }

        /* ESTILOS DE PERFIL (Para que coincida con la imagen) */
        .profile-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 20px;
        }
        
        .profile-container h3 {
            margin-bottom: 30px;
            color: #333;
        }

        .profile-card {
            width: 100%;
            max-width: 500px;
            background: white;
            padding: 40px;
            border-radius: 10px;
            text-align: center;
        }

        .profile-icon {
            margin-bottom: 25px;
            color: #3498db; 
        }

        .profile-form {
            text-align: left;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
            color: #555;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            background-color: #f9f9f9;
        }

        .form-group input:disabled {
            background-color: #eee;
            color: #777;
        }

        .password-note {
            font-size: 0.9em;
            color: #999;
            margin-bottom: 20px;
            text-align: center;
        }

        hr {
            border: 0;
            border-top: 1px solid #eee;
            margin: 25px 0;
        }

        .button-group {
            text-align: center;
            margin-top: 30px;
            display: flex;
            justify-content: center;
            gap: 15px;
        }

        .btn {
            padding: 12px 25px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .btn-save {
            background-color: #3498db;
            color: white;
        }

        .btn-save:hover {
            background-color: #2980b9;
        }

        .btn-cancel {
            background-color: #e74c3c;
            color: white;
        }

        .btn-cancel:hover {
            background-color: #c0392b;
        }

        /* --- Mensajes de Estado --- */
.alert {
    padding: 15px;
    margin-bottom: 20px;
    border-radius: 4px;
    font-weight: bold;
    text-align: center;
}

.alert-success {
    background-color: #d4edda; /* Verde claro */
    color: #155724; /* Verde oscuro */
    border: 1px solid #c3e6cb;
}

.alert-danger {
    background-color: #f8d7da; /* Rojo claro */
    color: #721c24; /* Rojo oscuro */
    border: 1px solid #f5c6cb;
}

    </style>
</head>
<body>
    
    <div class="sidebar">
        <div class="logo">
            PRINCIPAL
            DASHBOARD
        </div>
        
        <nav>
            <a href="/dashboard/usuarios" class="{{#if isUsersView}}active{{/if}}">
                <i class="fas fa-users"></i> Usuarios
            </a>
            <a href="/dashboard/perfil" class="{{#if isProfileView}}active{{/if}}">
                <i class="fas fa-user-circle"></i> Perfil
            </a>
        </nav>
        
        <div class="logout-section">
            <a href="/login" class="logout-btn">
                <i class="fas fa-sign-out-alt"></i> Cerrar Sesión
            </a>
        </div>
    </div>
    
    <div class="main-content">
        <div class="header">
            <h1>{{viewTitle}}</h1> 
        </div>
        
        <div class="dashboard-body">
            {{#if isProfileView}}
            <div class="profile-container">
                <h3>Configuración de Perfil y Contraseña</h3>

                {{#if successMessage}}
                <div class="alert alert-success">
                    <i class="fas fa-check-circle"></i> {{successMessage}}
                </div>
                {{/if}}

                {{#if errorMessage}}
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-triangle"></i> {{errorMessage}}
                </div>
                {{/if}}
                
                <div class="profile-card">
                    
                    <form class="profile-form" method="POST" action="/dashboard/perfil/update-password">
                        
                        <div class="form-group">
                            <label for="profile-nombre">Nombre:</label>
                            <input type="text" id="profile-nombre" value="{{user.nombre}}" disabled>
                        </div>
                        
                        <div class="form-group">
                            <label for="profile-email">Email:</label>
                            <input type="email" id="profile-email" value="{{user.email}}" disabled>
                        </div>

                        <hr>
                        
                        <p class="password-note">Para cambiar tu contraseña, ingresa la nueva y confírmala:</p>
                        
                       <div class="form-group">
        <label for="current_pass">Contraseña Actual:</label>
        <input type="password" id="current_pass" name="current_pass" placeholder="********" required>
    </div>

    <div class="form-group">
        <label for="new_pass">Nueva Contraseña:</label>
        <input type="password" id="new_pass" name="new_pass" placeholder="********" required>
    </div>

    <div class="form-group">
        <label for="confirm_pass">Confirmar Contraseña:</label>
        <input type="password" id="confirm_pass" name="confirm_pass" placeholder="********" required>
    </div>

                        <div class="button-group">
                            <button type="submit" class="btn btn-save"><i class="fas fa-check"></i> Cambiar</button>
                            <button type="button" class="btn btn-cancel"><i class="fas fa-times"></i> Cancelar</button>
                        </div>
                    </form>
                </div>
            </div>
            {{/if}}
        </div>
    </div>
    
</body>
</html>